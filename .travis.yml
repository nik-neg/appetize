language: node_js
node_js:
  - '14'
cache:
  yarn: true
  directories:
    - node_modules
before_install: cd ./client
install:
  - npm install
before_install: cd ./server
install:
  - npm install
script:
  - yarn test
